import { TodoWarningDirective } from './todo-warning.directive';

describe('TodoWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoWarningDirective();
    expect(directive).toBeTruthy();
  });
});
